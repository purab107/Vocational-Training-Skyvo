import { TruncateCustomPipe } from './truncate-custom-pipe';

describe('TruncateCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
